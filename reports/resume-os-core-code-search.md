# Resume OS core code-search audit

Required repository search was run after canonical migration.

## `AI.message(`

Remaining production occurrences:
- `web/src/views/Outreach.jsx` — outreach/message drafting only; never resume content.
- `web/src/views/Jobs.jsx` — separate recruiter/contact outreach draft only; the resume is generated by `Applications.generate()` → canonical Resume OS.

## `AI.resumeMessage(`

No production occurrences remain.

## `tailoredResume`

Remaining occurrences are compatibility/serialization fields in Jobs, the legacy ResumeTailor UI, the legacy HTTP response, the fabrication checker, and application-package response. The field now contains canonical ResumeDocument-derived text; it is not an AI response.

## `latexResume`

No production resume-authoring occurrence remains.

## `useAi`

- Resume OS HTTP schemas retain `useAi` only as a backward-compatible alias, default `false`, mapped to explicit `aiPolish`.
- Legacy narrative/writing-provider modules still contain an internal `useAi` option, but the canonical ResumeTailoringService forces `useAi:false` inside its AI-deny transaction.
- Old `writingProviders.assistRewrite()` remains for historical unit tests; the live `/api/resume-os/assist` route no longer calls it.

## `/tailor-v3`, `/tailor-for-job`, `/api/resume/tailor`

Endpoint names remain for compatibility. Their content-writing work delegates to the canonical Resume OS application service.

## `enhanceResumeNarrative(`

Called by the canonical ResumeTailoringService as its underlying Narrative Intelligence implementation. No product surface calls it independently.

## `assistRewrite(`

No live route caller. It remains as a historical utility covered by legacy tests.

## `proposeSelection(`

No live independent writer occurrence. Selection compatibility after canonical tailoring is presentation/ranking only.

## `runTailoring(`

Expected occurrences:
- Resume OS application service — canonical content entry.
- Canonical convenience wrappers — same service.
- `/api/resume-os/narrative/preview` — non-authoritative single-unit candidate inspection; does not persist or become a second product writer.

No hidden full-resume AI-authoring bypass was found in Jobs, Editor, Resume Studio, legacy tailoring API, or application-package resume generation.
