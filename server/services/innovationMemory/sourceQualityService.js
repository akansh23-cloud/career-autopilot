/* Innovation memory — source quality (re-exports the shared scorer so the
   memory module exposes the interface named in the spec). */
export { scoreSourceQuality, assessPrivacyRisk, computeSourceMix } from '../problemIntelligence/sourceQualityService.js';
export { default } from '../problemIntelligence/sourceQualityService.js';
